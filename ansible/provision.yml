---

- hosts: main
  become: yes
  gather_facts: no
  roles:
     - docker-engine
     - docker-volume
     - auto-deploy

- hosts: Main
  become: yes
  gather_facts: no
  roles:
    - { role: docker-images,
        message: "Docker Images for Main virtual machine",
        images: ["nginx:latest", "tomcat:latest", "portainer/portainer:latest"]
      }
